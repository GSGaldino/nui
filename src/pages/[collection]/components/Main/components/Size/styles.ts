import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex-direction: column;
  gap: var(--spacement-default);
  margin: var(--spacement-large) 0;
`;

export const Sizes = styled.div`
  display: flex;
  gap: var(--spacement-default);
`;
